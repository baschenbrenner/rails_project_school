Question:
<%= question.content %>
<br/>
Asked by <%= question.asked_by %> <br/>
<%= question.asked_when %>

<% if question.comments != [] %>

<ul>
   <% question.comments.each do |comment|%> 
   <li><%= comment.content %> posted by <%= comment.by %> on <%= comment.at %>  </li>
   <% end %>
   
      
</ul>
<% end %>
 <% if current_user.courses.include?(@course) %>
 <%= render partial: '/comments/new', locals: {q: question, } %>
 <% end %>